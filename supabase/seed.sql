-- Test data for local development
-- Note: auth.users records are created by Supabase Auth, not seed.sql
-- This file inserts corresponding profiles for testing

-- Example: insert a test profile (the auth user must be created first via Supabase Auth)
-- insert into public.profiles (id, display_name, preferred_lang)
-- values ('00000000-0000-0000-0000-000000000001', 'Demo User', 'zh')
-- on conflict (id) do nothing;
